/*
 *
 *    signup.js
 *
 */
//==============================================================================
$(document).ready(function() // when signup page DOM is ready, ...
{
      //TODO Get form data from signup page and validate user data, then send the data.

      /*
      var signUpButton = document.getElementById('signUp')

      function signUp() {

        var userName;
        var userPassword;
        var userEmail;

        var newUser = {
          name = userName,
          password = userPassword,
          userEmail = userEmail
        }

        // Send post request with new user's data.
        $.post( "/insertuser", newUser );
      }

      signUpButton.addEventListener('click', signUp, false);
      */
});
