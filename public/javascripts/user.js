/*
 *
 *    user.js
 *
 */
//==============================================================================
$(document).ready(function() // when user DOM is ready, set the data into elements.
{
  /*
      TODO
      Get all user data from:
      $.get( "/retreiveuser", function( userData ) {

          Set all data into html elements from userData
      }
  */
});
